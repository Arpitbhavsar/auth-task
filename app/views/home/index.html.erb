<div class="row">
  <div class="jumbotron">
    <h1 class="text-center">Welcome</h1>
    <%= link_to "View Your Details", profile_path(current_user.profile), class: "btn btn-primary btn-lg"  if user_signed_in? %>
  </div>

</div>